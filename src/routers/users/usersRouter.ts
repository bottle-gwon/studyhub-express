import express from 'express'

const usersRouter = express.Router()

usersRouter.get('/chat/rooms', async () => {
  // const page = Number(req.query.page ?? 1)
  // const page_size = Number(req.query.page_size ?? 5)
})
